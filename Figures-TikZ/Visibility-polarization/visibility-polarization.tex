\documentclass{standalone}
\usepackage{tikz}
\usetikzlibrary{decorations.markings,
decorations.pathmorphing,
arrows,
positioning,
calc,
patterns}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

\begin{document}

\definecolor{myRed}{RGB}{204,0,0}
\definecolor{myBlue}{RGB}{0,102,204}

\begin{tikzpicture}

  \begin{scope}
  \begin{axis}[
    height=6cm,
    width=10cm,
    domain=-pi/2:pi/2,
    samples=101,
    ymin=-0.1,
    ymax=1.25,
    xtick={-pi/2,-pi/4,0,pi/4,pi/2},
    xticklabels={$-\pi/2$, $-\pi/4$, $0$, $-\pi/4$, $-\pi/2$},
    xlabel={Polarization mismatch $\theta$},
    title={HOM interference for polarization mismatch},
    legend entries={visibility,coincidence prob.},
    legend columns=2,
    legend style={/tikz/every even column/.append style={column sep=0.5cm}}]

  \addplot [myBlue, very thick, smooth] {cos(deg(x))^2};
  \addplot [orange, very thick, smooth, dashed] {0.5*sin(deg(x))^2};

  \end{axis}
  \end{scope}

\end{tikzpicture}

\end{document}
